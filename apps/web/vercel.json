{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm run build --filter=@onlineusta/web",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "runtime": "nodejs20.x"
    }
  }
} 